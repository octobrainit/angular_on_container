# Sample to code angular on Container 

## After start your wsl and docker env run the commands bellow

```
docker-compose up -d --build 
docker exec -it app_angular bash
cd src
```
Now you are in the container, enjoy your project
